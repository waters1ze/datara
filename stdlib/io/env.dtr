class Env {
    prefix: Str
}

behavior Env {
    get(key: Str) -> Str {
        return env_get(key)
    }

    args_count() -> Int {
        return args_count()
    }

    arg(idx: Int) -> Str {
        return args_get(idx)
    }
}
