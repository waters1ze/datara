[package]
name = "http_server"
version = "1.0.0"
description = "Lightweight HTTP/1.1 protocol parser and response router in pure Datara"
authors = ["Datara Core Team <core@datara.dev>"]
entry = "main.dtr"

[target]
opt_level = "release"
