manifest_schema = 1

[package]
name = "strx"
version = "0.1.0"
description = "Extended string manipulation, padding, case conversion, and splitting utilities for Datara"
authors = ["Datara Core Team"]
license = "MIT OR Apache-2.0"
entry = "src/lib.dtr"
