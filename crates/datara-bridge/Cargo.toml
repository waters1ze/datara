[package]
name = "datara-bridge"
version = "1.0.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Zero-copy FFI bridge for embedding and interoping with Datara 1.0"

[lib]
crate-type = ["rlib"]

[dependencies]
